# Direct dependencies (pinned).
# For exact reproduction (transitive deps), use requirements-lock.txt.

PySide6==6.10.2
ortools==9.15.6755
pytest==9.0.2

# Build tooling (used by build.py to package the desktop binary).
pyinstaller==6.16.0
